---
import MainLayout from "../Layout/MainLayout.astro";
import BlogPost from '../components/BlogPost.astro';
const title = "Blog page";

const col = "purple";

const allPosts = await Astro.glob('../pages/posts/*.md');
---


		<style define:vars={{col}}>
			h1{
				/*just ignore this error because, it will work on the page*/
				color : var(--col);
			}
		</style>

     <MainLayout pagetitle="Blog Page">
        <h1>Blog page</h1>
        <h1>My Astro Learning Blog</h1>
        <p>This is where I will post about my journey learning Astro.</p>


         <!--for dynamic represent of post link -->
       <!-- <ul>
           {allPosts.map((post)=>
                   <li style="list-style: none;  border: 2px solid rgba(13,0,128,0.79); border-radius: 20px; text-align: center; margin: 15px; text-transform: capitalize;"><a href={post.url} style="text-decoration: none">
                       <img src={post.frontmatter.image.url} width="350px"/> <br> <h2> {post.frontmatter.title} </h2></a></li>     )}
        </ul>-->

         <ul>
             {allPosts.map((post) => <BlogPost url={post.url} title={post.frontmatter.title} />)}
         </ul>


	 </MainLayout>